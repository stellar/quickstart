name: Test

on:
  push:
    branches: master
  pull_request:

jobs:

  test:
    strategy:
      matrix:
        network: [standalone]
        dockerfile: [Dockerfile, Dockerfile.testing]
        options: ["", --enable-horizon-captive-core]
        exclude:
          - network: testnet
            options: --enable-horizon-captive-core
          - network: pubnet
            options: --enable-horizon-captive-core
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: Build Stellar-Core Image
      if: ${{ matrix.dockerfile == 'Dockerfile.testing' }}
      run: docker build git://github.com/stellar/stellar-core-experimental-cap21and40#cap21and40 -e CONFIGURE_FLAGS='--disable-tests' -f docker/Dockerfile.testing -t stellar/stellar-core:testing

    - name: Build Stellar-Horizon Image
      if: ${{ matrix.dockerfile == 'Dockerfile.testing' }}
      run: docker build git://github.com/stellar/go#cap21and40 -f services/horizon/docker/Dockerfile.dev -t stellar/stellar-horizon:dev

    - name: Build Quickstart Image
      run: docker build -f ${{ matrix.dockerfile }} -t stellar-quickstart .

    - name: Run Quickstart Image
      run: docker run --rm -d -p "8000:8000" --name stellar stellar-quickstart --${{ matrix.network }} ${{ matrix.options }}

    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: ^1

    - name: Sleep until supervisor is up
      run: sleep 10

    - name: Run test
      run: |
        echo "supervisorctl tail -f stellar-core" | docker exec -i stellar sh &
        echo "supervisorctl tail -f horizon" | docker exec -i stellar sh &
        go run test.go

spec:
  name: docker-stellar-core-horizon
  services:
  - dockerfile_path: Dockerfile
    git:
      branch: cap21and40
      repo_clone_url: https://github.com/leighmcculloch/stellar--docker-stellar-core-horizon.git
    name: docker-stellar-core-horizon
    run_command: /start --standalone --randomize-network-passphrase
